{
  "name": "ai-amana-functions",
  "private": true,
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions,firestore,auth,storage",
    "deploy": "firebase deploy --only functions",
    "lint": "tsc --noEmit"
  },
  "engines": {
    "node": "22"
  },
  "dependencies": {
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^6.4.0",
    "openai": "^6.10.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "~5.9.2",
    "@types/node": "^25.7.0"
  }
}
